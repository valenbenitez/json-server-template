{
  "name": "fake-api",
  "version": "1.0.0",
  "description": "Template for creating a backend using JSON Server",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "dev": "json-server --watch db.json",
    "start": "npm i && node json-server --watch db.json"
  },
  "dependencies": {
    "clone": "^2.1.2",
    "json-server": "^0.17.1"
  }
}
